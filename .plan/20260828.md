# 2026-08-28 每日增量计划

**今日项**:Phase 0 第 4 项 — 写 `scripts/import_price_history.py`(项目开发计划.md §五)

**做什么**:把 `data/prices.example.json`(6 SKU × 58 快照)标准化导入为 `data/price_history.json`。脚本做四件事:(1) 读快照 → 按 product_id 分组;(2) 校验字段(product_id/platform/price/original_price/date);(3) 排序 + 去重 + 输出归一化的历史价(每 SKU 数组按日期升序);(4) CLI `--stats` 模式输出每个 SKU 的快照数 / 时间范围 / 平台分布。复用 `lowest_price.py` 的 30 天最低价逻辑作为可选增强(后续 T 接)。

**涉及文件**:
- 新增 `scripts/import_price_history.py`(主脚本,100-150 行)
- 新增 `data/price_history.json`(归一化后的历史价,首次运行生成)
- 新增 `tests/test_import_price_history.py`(单测 8-10 个,断言分组/排序/校验/统计)
- 更新 `项目开发计划.md` 第 228 行 checkbox `[ ]` → `[x]`
- 不动 `消费顾问开发架构与计划.md`(关键项 1 等决策)

**验收**:
- `python3 scripts/import_price_history.py` 可直接运行,无报错生成 `data/price_history.json`
- `python3 scripts/import_price_history.py --stats` 输出每 SKU 快照数 / 平台 / 时间范围
- 输入缺字段(无 price / 无 date)→ 报错并跳过该条,不静默吞掉
- 单测 8+ 全过,验证分组、排序、校验、统计四个路径
