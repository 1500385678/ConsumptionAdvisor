# 2026-09-01 每日增量计划

**今日项**:README 补 1 节"决策辅助输出样例"(基于 `decision_helper.py` 21/21 PASS 落地)

**做什么**:README 工具链已记录 `decision_helper.py` 三种调用模式,但**没有贴出真实输出样例**——飞书 Bot 接入预演版的"看得到"短板。补 1 节"决策辅助输出样例",3 个典型场景(数码中端需要 / 服饰性价比想要 / 家电不限需要)+ 1 个值得分启发式对照表。T5 完成后 README 从 3,196 → ~5,500 字节,用户/张勇不用跑 CLI 也能看到报告长什么样。

**涉及文件**:
- 改 `README.md`(在"数据层"和"目录结构"之间新增"决策辅助输出样例"节)
- 不动 `scripts/decision_helper.py`(代码不动,只补文档)
- 不动 `项目开发计划.md` 主文件(L230 飞书 Bot 接入 checkbox 维持 `[ ]`,等张勇明示)

**验收**:
- README 新章节含 3 个场景的 markdown 输出 + 1 个值得分对照表
- 现有 80/80 单测继续 PASS(文档改动不影响)
- 决策辅助 CLI 三种模式跑出来的实际数据与 README 中展示的一致

**备注**:
- 关键项 1(`消费顾问开发架构与计划.md` 8 日 untracked)等张勇决策,不主动入库
- 关键项 2(0830 巡检报告缺)等张勇排期 T1 cron 排查,不主动修复
- 关键项 3(本 plan 文件缺失)本次顺手补,关 8 日断档
