# 2026-08-24 每日增量计划

**今日项**:Phase 0-1 — 写 `scripts/md_to_json.py`(项目开发计划.md §五)

**做什么**:把 md_to_json.py 脚本骨架落地,解析"消费品类清单.md"+"品牌评估框架.md" → `data/categories.json` + `data/brands.json`。脚本分两步:(1) 读 md → 标题/列表/表格三段式结构化解析;(2) 校验 + 写出 JSON,带 schema 校验。源文件暂缺,先实现解析器 + 内置 fallback 示例数据(10 大类 / 5 个种子品牌),后续 T5 替换为真实数据。

**涉及文件**:
- 新增 `scripts/md_to_json.py`(主脚本,150-200 行)
- 新增 `data/categories.json`(10 大类种子数据)
- 新增 `data/brands.json`(5 品牌种子数据)
- 新增 `data/schema.json`(JSON Schema 校验文件)
- 不动 `项目开发计划.md` 主文件

**验收**:
- `python3 scripts/md_to_json.py` 可直接运行,无报错生成 data/*.json
- 解析器支持三类 md 结构:一级标题、Markdown 列表、Markdown 表格
- JSON 通过 schema.json 校验
- 单元测试 1 个:解析器对小段 md 字符串返回正确结构
